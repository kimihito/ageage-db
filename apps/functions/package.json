{
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "watch": "tsc -w",
    "serve": "npm run watch | npm run firebase-serve",
    "firebase-serve": "firebase serve --only functions --host 0.0.0.0 --token ${FIREBASE_TOKEN}",
    "firebase-shell": "firebase functions:shell",
    "shell": "npm run watch | npm run firebase-shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "10"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^8.6.0",
    "firebase-functions": "^3.3.0",
    "firebase-tools": "^7.14.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^0.1.6",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  },
  "private": true,
  "version": "0.1.0"
}
